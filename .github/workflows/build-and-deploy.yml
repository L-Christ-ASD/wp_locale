---
name: Build and Deploy (Self-Hosted)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build and Deploy on Self-Hosted Runner
    runs-on: self-hosted  # Utilise un runner auto-hébergé

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Show containeur
        run: docker ps -a || true

      - name: Stop and Remove Old Container
        run: |
          docker compose down -v || true

      - name: Remove Old images 
        run: |
          docker image prune -a -f || true

      - name: Remove Old Container
        run: |         
          docker stop f690f3ce6edc 1a2eee80c11f 5163effec2da 317436bbf86a d7d172b4a971 384c776e1d99 
          docker rm f690f3ce6edc 1a2eee80c11f 5163effec2da 317436bbf86a d7d172b4a971 384c776e1d99
          docker container prune -f || true   
          
      - name: Run container
        run: |
          docker compose up -d || true

      - name: Check new containeur
        run: docker ps -a || true